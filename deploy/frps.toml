# frp 服务端配置（云服务器 Ubuntu 24.04）
# 路径：/etc/frp/frps.toml

bindPort = 7000

# NOTE: Dashboard 可选，用于查看连接状态
webServer.addr = "0.0.0.0"
webServer.port = 7500
webServer.user = "admin"
webServer.password = "changeme"
